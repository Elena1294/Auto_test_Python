Метод to_list не распознает другие разделители, кроме запятой
Метод starts_with не определяет первый символ вхождения в строку, если ищется один символ
Метод end_with не определяет последний символ строки, если ищется один символ
Метод is_empty не определяет пустоту строки при передаче ""